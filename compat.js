// Chrome/Edge have no `browser` global (Firefox has both, aliased to chrome.* under the hood). Every WebExtension call this app makes - cookies.get, storage.local, storage.session, tabs.create, runtime.getURL - is promise-native in Chrome MV3, so aliasing the name is enough; no polyfill dependency needed (grep-verified against every browser.* call site ). Loaded as a classic script BEFORE the module script in dashboard.html - classic-before-module is a guaranteed execution order, so this always runs first there. dev-preview.html loads its browser-stub.js (which defines window.browser itself) before this tag, so the check below no-ops there and the stub keeps winning.
if (typeof browser === 'undefined') globalThis.browser = chrome;
